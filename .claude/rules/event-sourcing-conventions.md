# Event Sourcing Conventions

Use the **`event-sourcing-builder`** subagent when creating or modifying Event Sourced features. It handles the full pattern: aggregates, commands, events, projections, projectors, command dispatch, and the web API layer.
