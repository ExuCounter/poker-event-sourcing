# UI Design Conventions

Use the **`ui-designer`** subagent when styling pages, polishing UI, adding components, writing Tailwind CSS, or working with JS hooks and assets. It handles Phoenix v1.8 layout conventions, Tailwind v4 import syntax, asset bundling rules, and design principles.
