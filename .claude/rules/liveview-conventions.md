# LiveView Conventions

Use the **`liveview-builder`** subagent when creating or modifying Phoenix LiveViews. It handles the full slice: LiveView module, HEEx template, router placement, streams, forms, phx-hooks, and ExUnit tests.
